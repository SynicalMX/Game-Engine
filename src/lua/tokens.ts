/**
 * tokens.ts
 * This file contains all the names of the tokens.
 */

export enum TokenType {
    IDENTIFIER,
    OPEN_PAREN,
    CLOSE_PAREN,
    OPEN_SQUARE,
    CLOSE_SQUARE,
    KEYWORD,
    PLUS,
    MINUS,
    DIVIDE,
    SUBTRACT
}

export enum KeywordType {
    AND,
    END,
    IN,
    REPEAT,
    BREAK,
    FALSE,
    LOCAL,
    RETURN,
    DO,
    FOR,
    NIL,
    THEN,
    ELSE,
    FUNCTION,
    NOT,
    TRUE,
    ELSEIF,
    IF,
    OR,
    UNTIL,
    WHILE
}